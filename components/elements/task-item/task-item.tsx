import {ITodo} from "../../../constants/interfaces/todo";

type TaskItemProps = {
    taskItem:ITodo,
    active:boolean,
};

export function TaskItem({taskItem, active}: TaskItemProps) {
    return (
        <div>

        </div>
    );
};